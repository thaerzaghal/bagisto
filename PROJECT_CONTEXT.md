# Project

A multi-tenant SaaS e-commerce platform built on top of Bagisto (open-source Laravel commerce). Each merchant ("tenant") gets an isolated Bagisto store — isolated database, isolated storage, isolated cache namespace, isolated queue context, isolated search index — provisioned on demand, reachable at its own subdomain, managed both by the merchant (their own Bagisto Admin) and by a platform operator (a separate central "Platform Admin" panel).

# Product Goal

Initial target vertical: **general retail** (electronics, clothing, accessories, toys, bookstores, general retail). NOT restaurants — restaurants are explicit future scope and must not shape current decisions.

Intended flow:

```
Platform Owner
    -> creates/manages Plans (entitlements, pricing)
Merchant
    -> becomes a Tenant (currently: CLI-provisioned only, no self-service yet)
    -> gets an isolated store (own DB, own subdomain)
    -> manages products/orders/customers/settings in their own Bagisto Admin
    -> storefront is publicly reachable at their subdomain
    -> operates under Plan entitlements/limits (e.g. product count)
    -> has a Subscription (lifecycle state, independent of billing)
    -> can optionally pay via Stripe (sandbox/reference only today)
```

Bagisto = the commerce engine. `packages/Platform/*` = the SaaS layer wrapped around it, with a hard rule: **`packages/Webkul/*` (Bagisto core) is never modified.**

# Technology

- Laravel 12 (12.62.0 confirmed live), PHP 8.3, MySQL 8.0, Redis (production cache/queue store).
- Bagisto 2.4.x, unmodified, in `packages/Webkul/*` (~42 packages, Concord + repository pattern).
- `stancl/tenancy` v3.10.1 — database-per-tenant isolation library (DB/cache/filesystem/queue bootstrappers all enabled and hardened).
- Custom SaaS code lives entirely in `packages/Platform/*`, its own PSR-4 namespace (`Platform\`), following the same "package = ServiceProvider + own Config/Routes/Models" shape Webkul packages use, but structurally separate so upstream Bagisto merges can never collide with it.
- Test framework: Pest 3. Platform's own suite lives in `tests/Feature/Platform/` (~22 files) plus a production-config "smoke" lane in `tests/Feature/PlatformSmoke/`.

# Architecture

**Central DB (`bagisto_central`) vs tenant DBs**: one central database holds `tenants`, `domains`, `plans`, `plan_features`, `plan_prices`, `subscriptions`, `payments`, `billing_provider_events`, `platform_users`, plus a central-only `sessions` table (used by Platform Admin's own login). Every tenant gets its own physical MySQL database, migrated with the full ~137-table Bagisto commerce schema (products, orders, customers, admins, its own `sessions` table, etc.) plus a couple of Platform-added tenant-schema tables. `platform:migrate:central` is the only supported way to run central migrations; a defense-in-depth guard (`PreventCentralMigrationOfTenantSchema`) blocks a bare `migrate` from accidentally sweeping Bagisto's schema into the central DB, and `CentralDatabaseWipeGuard` blocks `db:wipe`/`migrate:fresh`/`migrate:refresh`/`migrate:reset` from ever running against the real `bagisto_central` database (see INCIDENT-001 below).

**`packages/Platform/*` package boundaries and dependency direction** (one-way, enforced by absence of imports, not just convention):
```
Platform\Billing -> Platform\Subscriptions -> Platform\Plans -> Platform\Tenancy
Platform\Enforcement -> Platform\Plans (+ the one deliberate Webkul\Product dependency)
Platform\Admin -> Tenancy, Plans, Subscriptions, Billing (reads their central models directly)
```
- `Platform\Tenancy` — `Tenant` model/state machine, provisioning pipeline (`TenantProvisioner`), domain routing/access gate, suspension/reactivation, all isolation bootstrapping (cache/filesystem/queue/search retargeting listeners), the central-DB safety guards.
- `Platform\Plans` — `Plan`/`PlanFeature` models, `TenantEntitlements`/`TenantLimits` resolution, Plan CRUD, the tenant-facing "My Plan" page.
- `Platform\Subscriptions` — `Subscription` model/state machine (Trialing/Active/Canceled/Expired), `SubscriptionLifecycle` (the sole writer of `tenants.plan_id` via `TenantPlanAssignment`).
- `Platform\Billing` — `PlanPrice`/`Payment` models, provider-agnostic `PaymentProvider`/`WebhookVerifier` contracts, `StripePaymentProvider`/`StripeWebhookVerifier` reference adapter, checkout + webhook flow.
- `Platform\Enforcement` — the one package allowed to depend on a specific `Webkul\*` package; wires `products.limit` entitlement enforcement into real Bagisto extension points (`Product::creating`, a swapped-in import subclass).
- `Platform\Admin` — the central "Platform Admin" panel (guard `platform`, own login, own layout, zero Bagisto Admin asset dependency): tenant list/detail, provision/suspend/reactivate actions, plan/feature/price CRUD, subscription actions, recent-payments visibility.

**Tenant routing/isolation model**: subdomain-per-tenant. `Stancl\Tenancy\Middleware\InitializeTenancyByDomain` is prepended to the `web` middleware GROUP itself (`bootstrap/app.php`), so every Shop/Admin/API route automatically resolves the tenant from the Host header and swaps DB/cache/filesystem/queue/search context before any Bagisto code runs — zero Bagisto core changes needed. `Platform\Tenancy\Http\Middleware\TenantAccessGate` sits at the very front of that same group and fail-closed rejects any non-`Ready` tenant (423 for Suspended, 503 for everything else including unrecognized future statuses) *before* the tenant DB connection is ever opened. Platform Admin routes run under a separate `platform` middleware group (the `web` group's own resolved middleware, minus `InitializeTenancyByDomain`) plus `EnsureCentralDomain` — structurally incapable of ever touching a tenant DB connection, not just by convention.

**Plans/entitlements**: `Plan` (code/name/is_active/sort_order) has many `PlanFeature` (feature_code/type[boolean|numeric|unlimited]/value). `TenantEntitlements`/`TenantLimits` resolve a tenant's current plan fresh on every read (no cache), so a plan change takes effect on the very next request. `tenants.plan_id` is a synchronized pointer, written exclusively by `TenantPlanAssignment::assign()` — the single source of truth for "what plan does this tenant have right now" that both manual admin changes and subscription-driven changes funnel through, so they can never disagree.

**Subscriptions**: one `Subscription` row per tenant (`unique(tenant_id)`), independent state machine from `TenantStatus` (canceling a subscription does NOT suspend the tenant; suspending a tenant does NOT touch its subscription — verified, not assumed). `SubscriptionLifecycle::start()/startTrial()/changePlan()/cancel...()` is the sole entry point; every plan-affecting transition calls `TenantPlanAssignment::assign()` internally.

**Billing**: `PlanPrice` (separate table from `Plan` — `billing_interval`/`interval_count`/`amount_minor`/`currency`/`is_active`) and `Payment` (historical snapshot of amount/currency at creation, never re-derived). `PaymentProvider`/`WebhookVerifier` are provider-neutral contracts; `StripePaymentProvider`/`StripeWebhookVerifier` are the one reference implementation (Stripe Checkout in `mode: 'payment'`, never `'subscription'` — Stripe never becomes a second subscription-lifecycle source of truth). Checkout → webhook → `WebhookEventProcessor` (one DB transaction, idempotent via a `unique(provider, provider_event_id)` row) → `PaymentLifecycle::markSucceeded()` → `SubscriptionLifecycle::changePlan()`. The browser success/cancel redirect NEVER marks a payment succeeded — only a verified webhook does.

# Current Capabilities

**Fully built and tested** (see `IMPLEMENTATION_PLAN.md`/`RISK_REGISTER.md` for the evidence trail):
- Tenant model/state machine, CLI-driven provisioning (`tenant:provision`), full isolation (DB/cache/filesystem/queue/search — each independently proven with real HTTP requests, real queued jobs, real Redis).
- Domain routing (subdomain → correct tenant DB), tenant access gate (suspended/pending/failed/etc. all correctly blocked before DB access).
- Platform Admin: login, dashboard, tenant list/detail, provision/retry/suspend/reactivate, plan/feature/price CRUD, subscription actions, recent payments.
- Tenant Admin: "My Plan" page (plan + subscription + upgrade link), real Bagisto Admin otherwise completely stock.
- Plans/entitlements/product-limit enforcement (Admin UI path AND bulk CSV import path both enforced).
- Subscriptions (Trialing/Active/Canceled/Expired, manual transitions only, no scheduler).
- Billing: Stripe sandbox checkout + webhook, signature-verified, idempotent, wired to `SubscriptionLifecycle::changePlan()`.
- CI: all `.github/workflows/*.yml` are `workflow_dispatch`-only (TASK-MVP-011) - local-first testing is the official policy, GitHub is source/history storage only. See "Local Development / Testing" below.

**Built since the section above was last accurate (TASK-MVP-001/002/003/004A)**:
- **Merchant self-service signup — DONE (TASK-MVP-001).** `packages/Platform/Signup`: public `/join` form creates a Tenant + Domain, triggers `TenantProvisioner`, gives the merchant a real self-chosen owner email/password (never a hardcoded seeded default), with signed-URL retry authorization for provisioning failures. R19 resolved at this exact input boundary (slug allowlist regex).
- **Shopper order flow — VERIFIED end-to-end (TASK-MVP-002).** Real browse → cart → checkout → order-placed flow proven against a real tenant storefront (Cash on Delivery), fully tenant-isolated, inventory reservation confirmed, newly-provisioned-merchant usability confirmed (no provisioning gap).
- **First-run merchant readiness — VERIFIED (TASK-MVP-003).** Bagisto Admin confirmed already sufficient for all core merchant capability; one real provisioning gap found and fixed (channel hostname defaulting to the central URL instead of the tenant's own domain); small welcome banner added on first post-signup login.
- **Production application hardening — DONE (TASK-MVP-004A).** `TRUSTED_PROXIES` (closes R20 at the application level), `PLATFORM_CENTRAL_DOMAINS` (explicit, separate from `PLATFORM_BASE_DOMAIN`), a clean Stripe-unavailable checkout UX fix, `platform:production:check`, and `docs/architecture/production-deployment.md`. Actual infrastructure (real domain/server/DNS/TLS/SMTP/Redis, backup scheduling) remains TASK-MVP-004B, not yet started.
- **Real pilot deployment — DONE (TASK-MVP-004B/TASK-MVP-003A/003B).** A real Oracle Cloud server, real domain (`technify.dev`), real TLS, and a real, restore-proven daily backup system are live. A single real pilot tenant (`pilot-smoke`) has been through a full real signup → dashboard → checkout smoke test. Several real production-only defects were found and fixed live (RISK_REGISTER.md R53-R63) - see that file for the full list; two narrow, low-severity ones (R61/R62) remain deliberately open.
- **Real email/SMTP delivery — DONE (TASK-MVP-004).** Central SMTP now points at a real provider (Zoho Mail, `support@technify.dev`) via ordinary `.env` values - `Webkul\Core\Mail\Transport\DynamicSmtpTransport` (unmodified Bagisto) already provided the tenant-override/central-fallback behavior needed, so no Platform code was required. All three required real-world proofs (a diagnostic send, a real merchant password-reset - including the human browser journey - and a real shopper order-confirmation) were completed with real external Gmail receipt confirmed by the operator. Found and fixed one real, unrelated production defect along the way (R63 - see below).
- **Off-server backup sync — DONE (TASK-MVP-005).** New `Platform\Backup\Contracts\OffsiteBackupDestination` (provider-neutral) + `S3CompatibleOffsiteDestination` (Laravel's own `Storage::build(['driver' => 's3', ...])`, Cloudflare R2 as the current configured provider, not architecture lock-in) sync already-finalized local backups to an independent offsite destination (`platform:backup:sync-offsite`/`platform:backup:cleanup-offsite`), closing R64 (same-server-only backups - now CLOSED). Proven live against the real pilot server and a real R2 bucket: real sync, independent remote listing, full checksum round-trip verification, idempotent re-sync, a real restore drill (central + tenant DB + tenant files, into disposable databases/directories, all cleaned up afterward), and a confirmed-private bucket (unauthenticated access rejected). The pilot server can now be completely lost while an independently restorable backup survives on R2.
- **Production deployment source-of-truth safeguards — DONE (TASK-MVP-006, RISK_REGISTER.md R65).** A real production regression (R63's fix silently reverted by a later, unrelated, legitimate image rebuild that read from a stale host source tree) is now closed with a durable process rule (`docs/architecture/production-deployment.md` "Deployment source of truth") plus two narrow `platform:production:check` additions: an `asset_helper_tenancy` FAIL guard and a `Deployed source`/`APP_COMMIT` visibility row.
- **Public signup abuse protection — DONE, proven live in production (TASK-MVP-006, RISK_REGISTER.md R66).** `Platform\Signup\Services\TurnstileVerifier` gates `POST /join` before any provisioning side effect, fails closed when enabled. `TURNSTILE_ENABLED=true` in production with real Cloudflare Turnstile (Managed widget) credentials configured by the operator; `POST /join`'s throttle tightened to `throttle:3,1`. Proven with real production traffic, not just automated tests: a real missing-token POST was rejected with zero Tenant/Domain/database/Subscription side effects; the operator personally completed a real Turnstile challenge in a real browser and a real tenant (`test1`) was fully, correctly provisioned as a direct result (verified server-side: Ready status, 138-table schema, correct Subscription/plan linkage, exactly one Admin, no duplicates); `pilot-smoke` confirmed unaffected throughout. Local dev/tests keep `TURNSTILE_ENABLED=false` (default), the correct posture there. Email-ownership verification was evaluated and explicitly deferred (DECISION_LOG.md C77) - Turnstile + throttle are judged sufficient for the first Public MVP. Observed real signup-to-redirect latency (~20-30s, synchronous provisioning) is a disclosed, accepted UX limitation, not a defect - see "Post-MVP / explicitly deferred" below.
- **Managed merchant onboarding / public signup disabled by default — DONE (TASK-MVP-007).** Deliberate product decision, not a temporary hack: public self-service signup is now OFF in production (`PUBLIC_SIGNUP_ENABLED=false`, `config('platform.signup.enabled')`) for the initial commercial/pilot phase - limited server capacity, real cost per provisioned tenant, and small expected initial merchant count make curated onboarding the right posture. `GET`/`POST /join` genuinely 404 (zero side effects, `Platform\Signup\Http\Middleware\EnsurePublicSignupEnabled`) when disabled; the signed retry flow is deliberately unaffected either way. Platform Admin gained a real "Create Merchant" flow (`TenantController::create()`/`store()`/`resendActivation()`) that reuses `MerchantOnboarding::register()` verbatim - same provisioning pipeline `/join` itself uses, never a second implementation. The operator never sees or chooses a merchant password: a random temporary password is generated, used once to seed the owner Admin row, then discarded - the merchant's own real, already-proven Admin password-reset broker (TASK-MVP-004) sends them a real activation email to set their own password. Turnstile, email verification, and async provisioning are all explicitly untouched/still-deferred - see `docs/architecture/onboarding.md` for the full record. 27 new tests (`PublicSignupFlagTest.php` - 6, `ManagedMerchantCreationTest.php` - 17, 4 new rows in `ProductionReadinessCheckTest.php`), plus `PlatformSignupTest.php`/`SignupTurnstileTest.php` updated to explicitly opt back into public signup for their own fixtures now that it defaults off. Two real production regressions were found live during this task's own production verification and fixed as separate, narrow, documented hotfixes, not folded silently in: R67 (`EnsurePublicSignupEnabled`'s own `abort(404)` crashed under real `APP_DEBUG=false` - fixed, returns a direct response instead) and R69 (the managed-onboarding activation email linked to the central domain instead of the tenant's - fixed via a scoped `URL::forceRootUrl()`). Both proven live in production with the real `mvp007-check` tenant, operator-confirmed end-to-end (email received, correct tenant-domain link, password set, tenant Admin login succeeded).
- **Central-host exception rendering safety — DONE, proven live in production (TASK-MVP-008, RISK_REGISTER.md R68).** A real, broader-than-signup production defect found as a byproduct of R67: `Webkul\Core\Exceptions\Handler` forces any HTTP exception status outside `{401,403,404,503}` (405/419/422/429/500) into a themed error view that unconditionally queries tenant-only tables - crashing with a raw 500 for any request where tenancy was never initialized (any central Platform route, or a route matching no pattern for its HTTP verb). Fixed via a new, Platform-owned `Platform\Tenancy\Exceptions\CentralSafeExceptionHandler`, decorating whatever handler Webkul bound via `Container::extend()` - renders `TokenMismatchException`/`HttpExceptionInterface` directly under `APP_DEBUG=false` + `!tenancy()->initialized` + no self-rendering exception (excluding R57/R58's own `TenantNotReadyHttpException` by design); everything else is delegated first, falling back to a generic safe response only on a secondary failure (corrected mid-task after the first version was found live in production to incorrectly 500 unauthenticated Platform Admin requests - `Illuminate\Auth\AuthenticationException` is already safely handled by Webkul without ever touching a themed view). Zero `packages/Webkul/*` changes. R51/R53/R54/R57/R58 all confirmed unaffected, plus real production verification (419/405/429, unknown-domain, `pilot-smoke`, Platform Admin, `platform:production:check` all-PASS). R62 (a separate, pre-existing, unknown-domain eager-controller-construction crash) remains explicitly open. A genuinely separate, pre-existing, low-severity defect (R70 - Platform Admin's unauthenticated redirect lands on the shop customer login instead of its own) was found as a byproduct and is explicitly NOT fixed here.
- **Arabic-first merchant & storefront experience — DONE, DEPLOYED, PRODUCTION-VERIFIED, CLOSED (TASK-MVP-012, deployed/verified by TASK-MVP-014).** Every newly provisioned tenant now gets Arabic (`ar`, RTL) as its default storefront and Merchant Admin locale automatically, English preserved as a secondary locale, zero manual switching required. Built on Bagisto's own already-shipped Arabic translations and already-wired RTL support (dormant only because every tenant was previously seeded `en`-only) via three Platform-owned additions: `TenantProvisioner::ensureArabicLocaleSeeded()`/`ensureArabicThemeContentSeeded()` (idempotent provisioning steps), a new `Platform\Tenancy\Http\Middleware\SetTenantAdminLocale` (Admin follows the tenant's own channel default locale - no independent Admin-locale setting), and a locale capture/set/restore fix in `OwnerActivationMailer` (mirroring the R69/C81 URL-root idiom). `config/translatable.php`'s fallback locale changed to `ar`. Zero `packages/Webkul/*` changes. Full design: `docs/architecture/localization.md`. Found and fixed within the same task: a theme-content per-locale seeding gap (RISK_REGISTER.md R71, CLOSED). Found as a byproduct, NOT caused by this task, fixed and closed separately by TASK-MVP-013 before deployment: RISK_REGISTER.md R72 (`LimitExceededException` 422-rendering regression).
  **Production-verified (TASK-MVP-014, real Oracle Cloud server, real `arabic-mvp-check` tenant created through the real Platform Admin managed-onboarding HTTP flow - never `/join`, never a direct service call, never raw SQL):** default locales `[ar, en]` with `ar.direction=rtl` and the channel default set to `ar`; Merchant Admin renders `lang="ar" dir="rtl"` across login/dashboard/products/categories/orders/settings (human-confirmed login + independently re-verified across all six); storefront renders `lang="ar" dir="rtl"` across homepage/category/product/cart; a real Arabic category (`فئة تجريبية عربية`) and product (`منتج تجريبي عربي`) were created through the real Admin UI with zero English content required or auto-created; English confirmed genuinely available as a secondary, switchable locale (`?locale=en` → `lang="en" dir="ltr"`) without altering the tenant's own Arabic default; the activation/reset email's language and tenant-domain host were both human-confirmed correct; all four pre-existing English tenants (`pilot-smoke`/`test1`/`thaertest`/`mvp007-check`) re-verified completely unchanged (status, plan, locale, product counts) both before and after. `platform:production:check` all-PASS throughout.

- **Merchant onboarding checklist / first-real-merchant readiness — DONE (TASK-MVP-015).** `TenantController::show()`/`tenants/show.blade.php` gained owner name/email, clickable storefront/Merchant Admin URLs, and a small DERIVED-ONLY "Onboarding Status" panel (zero new persistence). New canonical `docs/operations/merchant-onboarding-checklist.md` (10 sections, `[Automated]`/`[Operator]`/`[Merchant]`/`[Verify]` tagged, includes an Arabic handoff message template). Zero `packages/Webkul/*` changes.
- **Palestine readiness / defaults — DONE (local implementation), NOT YET DEPLOYED (TASK-MVP-016).** Every newly provisioned tenant is now Palestine-first, not just Arabic-first: ILS as the only currency (no USD secondary yet - deliberately deferred, see DECISION_LOG.md C88), the 16 real governorates of the State of Palestine seeded as address states (`Platform\Tenancy\Support\PalestineGovernorates`), `channels.timezone = Asia/Hebron` (global `APP_TIMEZONE` untouched, stays `UTC` - C90), postcode requirement off, and Cash on Delivery enabled by default with Money Transfer explicitly deactivated until a merchant's real bank details are configured (found live by this task's own regression test that BOTH are actually active-by-default out of the box via a separate Webkul config file - Money Transfer's `active=0` write is required, not optional - see `docs/architecture/palestine-readiness.md`). `config('app.default_country')` set globally to `PS` - an explicit, disclosed Palestine-first market assumption, not a per-tenant setting (C89) - see `docs/architecture/palestine-readiness.md` for the full design, the authoritative governorate list/source, and three formally-recorded, deliberately-deferred findings (RISK_REGISTER.md R73 mail-sender identity, R74 storefront state-dropdown locale-blindness, R75 Admin Orders DataGrid timezone gap). Zero `packages/Webkul/*` changes. Existing Ready tenants (`pilot-smoke`/`test1`/`thaertest`/`mvp007-check`/`arabic-mvp-check`) are structurally unaffected (`provision()`'s own Ready-early-return). No tax rate, shipping fee, or payment gateway credential was set - explicit, intentional "no legal/business assumption" policy.

- **Eliminate app/web production asset drift — DONE, COMMITTED, PUSHED, DEPLOYED, PRODUCTION-VERIFIED (TASK-MVP-017, RISK_REGISTER.md R76, DECISION_LOG.md C92).** A real production incident, discovered during TASK-MVP-016's own verification (Merchant Admin Login/Reset Password inert across every tenant for 10 days - `web`, nginx, silently serving a stale build because it was never rebuilt while `app` was redeployed 3 times; `Dockerfile.production` copies `app`'s `public/` into `web` at BUILD time, never a runtime-shared volume), is now closed with two durable safeguards: (1) a new canonical `docker/production/deploy.sh` - always builds/recreates `app` AND `web` together (`--no-deps`, so `mysql`/`redis` are never touched), preserves R65/C76's own source-first rule unchanged, and runs `platform:production:check` as its own final gate (a failed check fails the script's own exit code too, no automatic rollback); (2) two new `platform:production:check` rows, `Admin static assets`/`Shop static assets`, which read each theme's real Vite manifest (never a hardcoded filename) and prove the current CSS+JS entry-point files are actually SERVED by `web` via a real internal `http://web/...` request - FAIL only on the precise incident condition (reachable `web`, non-200), WARN if `web` is unreachable at all (correct for environments with no app/web split, e.g. local dev), INFO if no manifest exists. Zero `packages/Webkul/*` changes; zero business-functionality change. 22 tests (16 pre-existing + 6 new) in `tests/Feature/Platform/ProductionReadinessCheckTest.php`, all passing locally.

- **Production Docker disk hygiene — DONE, COMMITTED, PUSHED, CLOSED (TASK-OPS-018/TASK-OPS-019, RISK_REGISTER.md R77, DECISION_LOG.md C93).** TASK-OPS-018 found production's root filesystem at 94% used (~30GB under `/var/lib/docker`, a real, live capacity risk) via a strictly read-only audit, then performed an approved, targeted cleanup (`docker builder prune -f` + `docker image prune -f`, no `-a`, no volumes) that physically reduced usage to 76% - proven safe via before/after container-identity checks (mysql/redis untouched) and a passing `platform:production:check`. TASK-OPS-019 then root-caused and fixed the underlying mechanism rather than relying on cleanup alone: `Dockerfile.production`'s `assets`/`app` stages both ran `COPY . .` (the whole repository) BEFORE `npm install`/`composer install`, invalidating those expensive layers' cache on nearly every deploy regardless of whether dependencies actually changed - confirmed directly from TASK-MVP-017's own real build transcript. Each theme's `package.json` and root `composer.json`/`composer.lock` are now copied and installed BEFORE the full source `COPY`, proven with a real local two-build test (a source-only second build showed `npm install`/`composer install` all `CACHED`). `docker/production/deploy.sh` gained a fail-closed, Docker-root-aware pre-build disk gate, a shared `flock` concurrency lock (protects against the scheduled hygiene backstop colliding with an in-progress deploy), a one-deploy-consistent rollback pair (`estore-app:rollback`/`estore-web:rollback` + `ROLLBACK_COMMIT`, advanced only after full success, cross-validated after writing with defensive restore-on-failure), and bounded (age-filtered, `until=168h`, never unconditional) post-deploy cleanup delegated to a new shared `docker/production/docker-disk-hygiene.sh` script. **First real production deployment (commit `bd3e2b4826f1cbf2059bd3eef360db267207fa79`) directly verified all of the above live**: app/web rebuilt+recreated, mysql/redis proven untouched, `platform:production:check` all-PASS including live Admin/Shop static-asset checks, Platform Admin + two Merchant Admin tenants + two storefronts all verified 200 with every current asset independently confirmed 200 via real HTTP, `estore-app:rollback`/`estore-web:rollback`/`ROLLBACK_COMMIT` confirmed to exactly equal the pre-deployment `:latest` pair/`APP_COMMIT`, and the `flock` lock proven correct in both directions (holding it, and a standalone hygiene run detecting genuine contention and skipping gracefully). Root filesystem improved 76%→66%; the first hygiene run reclaimed 8.67GB of PRE-EXISTING backlog cache (accumulated before this policy existed - not representative of steady-state per-deploy growth). A daily hygiene cron backstop (04:00, `/opt/estore/app/docker/production/docker-disk-hygiene.sh`, with a dedicated logrotate config) is now installed on the production host. Zero `packages/Webkul/*` changes; zero business-functionality change. **R77 is now CLOSED**: TASK-MVP-018's own real, source-changing production deployment (commit `a283d25ea944f85dd91936da33a648a35cce711f`) supplied the required subsequent-deploy evidence - dependency-install layers all reported `CACHED`, disk usage stayed exactly flat (66%→66%, not merely "improved"), builder prune correctly reclaimed 0B (nothing past the 7-day threshold yet), image prune removed only one genuinely dangling image, rollback metadata advanced correctly, mysql/redis confirmed untouched. This closes the specific "does usage stay bounded across real deploys" criterion, not a claim that disk usage can never grow again - a deploy that genuinely changes `composer.lock`/`package.json` will still correctly rebuild those layers, and the daily cron backstop remains in place regardless.
- **Per-tenant transactional mail sender identity — IMPLEMENTED, COMMITTED, PUSHED, ONE PRODUCTION DEPLOYMENT COMPLETE, A REAL BUG FOUND AND FIXED, PENDING RE-DEPLOYMENT (TASK-MVP-018, RISK_REGISTER.md R73).** Audit found the "From: Technify" defect was never missing architecture: `Webkul\Core\Core::getSenderEmailDetails()` (unmodified `packages/Webkul`) already reads a tenant-scoped `core_config` row (`emails.configure.email_settings.sender_name`), already consumed by every Bagisto order/invoice/shipment/refund/cancellation Mailable and the customer/Admin password-reset Notifications - the row simply had never been written, since populating it has always required a human to save it through Admin Configuration. Fix seeds it automatically: `TenantProvisioner::seedSenderIdentity()` (idempotent, never overwrites an existing value) is called from `MerchantOnboarding::attempt()` at the point a tenant's real store name first becomes known (managed onboarding only - public `/join`/CLI provisioning pass no store name and are unaffected, an intentional, disclosed limitation), plus a backfill command `platform:tenants:repair-sender-identity` (mirrors `platform:tenants:repair-channel-hostname` - `--tenant` selector, Ready-only, skips a tenant whose channel name is still Bagisto's own generic seeded placeholder, never run automatically). **Model A only, explicit product decision:** only the sender DISPLAY NAME is ever written - `sender_email` is never touched, so the already-proven-deliverable `support@technify.dev` remains the sending address; no Reply-To (`owner_email` is unverified), no merchant-domain spoofing, no DNS/SPF/DKIM/DMARC work. Pulled forward deliberately before the first real merchant onboarding (see DECISION_LOG.md). **Real production verification (first deployment) found a genuine bug the tests never caught**: a real `palestine-mvp-check` password-reset email still showed "From: Technify" after the `core_config` row had been correctly written, because the original raw `DB::table('core_config')->insert()` write bypassed Prettus L5 Repository's cache-invalidation events - `config/repository.php` (stock Bagisto, not this project's own config) enables repository-level caching specifically for `CoreConfigRepository`, invalidated only by `create()`/`update()`'s own dispatched events. Corrected: `seedSenderIdentity()` now writes through `CoreConfigRepository::create()` itself - the same method the real Admin Configuration save UI already uses - so the same invalidation fires; 2 new regression tests (16/17) reproduce the exact production read-then-seed ordering and would have caught this. 17 new focused tests (`tests/Feature/Platform/TenantMailSenderIdentityTest.php`) plus 69 existing regression tests all pass. Zero `packages/Webkul/*` changes. **R73 stays OPEN - the fix is committed/pushed but not yet re-deployed; `palestine-mvp-check` also needs one documented, not-yet-executed, narrow production cache invalidation (see docs/implementation/email-delivery.md) since its already-existing row means the fixed code path's own idempotency guard will skip it.**

**Still NOT built**:
- Merchant onboarding wizard beyond the one-time welcome banner, store branding beyond stock Bagisto Admin settings, custom-domain-per-tenant workflow — none exist (subdomain-only, single domain per tenant).
- **Actual production infrastructure (TASK-MVP-004B)**: no real domain/server has been chosen or provisioned, no real DNS/TLS/SMTP/Redis exists anywhere outside Docker-local, no backup script/schedule exists yet (policy documented, not implemented).

# Important Invariants

- Never modify `packages/Webkul/*` — verify via `git diff --name-only -- packages/Webkul/` (must be empty) after every task.
- All custom code lives in `packages/Platform/*`.
- Database-per-tenant; central DB never receives tenant commerce schema, tenant DBs never receive central schema (`platform:migrate:central` / `TenantProvisioner::ensureMigrated()` are the only supported paths).
- `bagisto:install` is **unsupported** against this project's central database — it wipes tables via internal `db:wipe`/`migrate:fresh` calls (see INCIDENT-001). `CentralDatabaseWipeGuard` blocks this at the `db:wipe`/`migrate:fresh`/`migrate:refresh`/`migrate:reset` level whenever the active database resolves to the literal, hardcoded name `bagisto_central`.
- Platform Admin routes structurally never initialize tenancy (by middleware-group construction, not by discipline).
- `tenants.plan_id` is written exclusively by `TenantPlanAssignment::assign()` — never set directly anywhere else.
- `SubscriptionLifecycle` and `TenantStatus`/`TenantLifecycle` are fully independent state machines with zero automatic coupling.
- Payment redirect (browser success/cancel URL) never proves payment success — only a signature-verified webhook does.
- Webhook/provider event processing is idempotent via a database-level `unique(provider, provider_event_id)` constraint, and wraps its entire effect (event row, Payment, Subscription plan change) in one DB transaction.
- Money is always an integer minor-unit value (`amount_minor`), never a float; currency is a plain normalized string, no hardcoded currency allowlist.
- Stripe is a reference/sandbox adapter only — not a production commitment. A real payment gateway (likely a Palestinian bank/provider) remains an open, deliberately deferred business decision; `PaymentProvider` exists specifically so that choice costs nothing architecturally later.
- Test fixtures: `PlatformIntegrationTestCase` disables `DatabaseTransactions` for its files — shared fixture tenants (`tenant-a`/`tenant-b` etc.) persist real state across test runs; never use a shared fixture tenant as a scratch/smoke-test subject (this has caused real, repeated regressions — see RISK_REGISTER R40).
- **Production deployment source of truth: the host source tree at `/opt/estore/app/` on the real pilot server, never a running container's writable layer.** A `docker cp`/`docker exec` patch into a live container is a legitimate fast path to restore service, but is never "done" until the identical change also lands in that host directory — a later, unrelated image rebuild (`docker compose build`) reads ONLY from there and will silently discard any fix that only ever lived in a container layer (this happened for real — see RISK_REGISTER R65). Every deploy now writes `git rev-parse HEAD > APP_COMMIT` into the deployed tree so `platform:production:check` can report what commit is actually running.

# Local Development / Testing

- Docker: `bagisto-mysql-1` (root/`password`, app user `sail`/`password`), `bagisto-redis-1`.
- Tests run via: `MSYS_NO_PATHCONV=1 docker run --rm --network bagisto_sail -v /e/Dev/EStore/bagisto:/var/www/html -w /var/www/html bagisto-spike-php:8.3 vendor/bin/pest ...`
- Bootstrap a fresh environment: `platform:mark-installed` → `platform:migrate:central` → `platform:plans:seed` → (optionally) `tenant:provision {id} --domain=...`. **Never** `bagisto:install` against the real central DB.
- Full Platform suite: `vendor/bin/pest tests/Feature/Platform` (273 tests as of the last completed task, ~13 minutes).
- Production-config smoke lane (`SESSION_DRIVER=database`/`CACHE_STORE=redis`/`QUEUE_CONNECTION=redis`/`RESPONSE_CACHE_ENABLED=false` — catches bugs the default test config masks, e.g. R29/R33): `vendor/bin/pest -c phpunit.smoke.xml`.
- **GitHub Actions: local-first testing policy (TASK-MVP-011).** Every workflow under `.github/workflows/` (`pest_tests.yml`, `pint_tests.yml`, `admin_playwright_tests.yml`, `shop_playwright_tests.yml`, `translation_tests.yml`, `platform_tests.yml`, `docker_publish.yml`) is `workflow_dispatch`-only - none run automatically on `push`, `pull_request`, or (for `docker_publish.yml`, previously) a `v*` tag push. GitHub is source/history storage; local execution is the real verification loop; production is verified only through safe, non-destructive checks (`platform:production:check`, safe HTTP smoke, backup/R2/SMTP health) - never the full Pest or Playwright suite. Every workflow file/job/service/command is otherwise unchanged and remains runnable on demand from the Actions tab. Full local command reference, the three testing levels, and known local-test caveats: `docs/implementation/ci-testing.md`; decision rationale: `DECISION_LOG.md` C83.
- No passwords/secrets in this file or in git; `.env` stays untracked (verify with `git ls-files .env` — must be empty).

# Current Git Baseline

Branch `2.4`, HEAD `c2a0773d9c25d3df76ddb2cc7bad3ea08416a575`, matching `origin/2.4` exactly (0 ahead, 0 behind - pushed). This HEAD is the exact commit deployed to and verified live in production (`platform:production:check`'s own `Deployed source` row confirms it). TASK-MVP-007 through TASK-MVP-014 are all **COMPLETED** - see "Remaining MVP Roadmap" below for the full per-task breakdown.

Current production posture:
- `PUBLIC_SIGNUP_ENABLED=false` - managed Platform Admin onboarding ("Create Merchant") is the active merchant-acquisition path.
- Turnstile remains implemented and ready for whenever public signup is re-enabled.
- Email/password-reset delivery is live (real SMTP), including the tenant-domain-correct activation link (R69 fixed) and the tenant-locale-correct activation language (Arabic-first tenants, TASK-MVP-012).
- New tenants are Arabic-first by default (`ar`/RTL, `en` secondary) - production-verified via the real `arabic-mvp-check` tenant (TASK-MVP-014). Existing English-only tenants (`pilot-smoke`/`test1`/`thaertest`/`mvp007-check`) are unaffected and never auto-migrated.
- `EntitlementException` (product-limit-exceeded and related plan/feature failures) renders a clean 422, not a raw 500 - fixed and production-verified (TASK-MVP-013, RISK_REGISTER.md R72).
- Local and R2 offsite backups are both healthy and restore-proven.
- Production `APP_COMMIT` matches the deployed source tree exactly.
- `packages/Webkul/*` remains completely untouched across the entire commit range.

This is a point-in-time baseline — check `git log`/`git status` fresh rather than trusting this file if it's been a while.

# Completed Foundation

Nineteen-plus architecture tasks (TASK-ARCH-001 through TASK-ARCH-019, plus INCIDENT-001 recovery) built, in order: tenancy feasibility → tenant model/provisioning → domain routing → cache/filesystem/queue/search isolation → plan/entitlement domain → tenant admin plan display → platform admin foundation → product-limit enforcement (+ bulk-import bypass closure) → tenant suspension → full tenant readiness access gate → plan/feature CRUD in Platform Admin → provider-agnostic subscription domain → CI + production-config smoke testing (interrupted by INCIDENT-001, recovered, resumed) → CI stabilization → provider-agnostic billing domain + Stripe reference adapter → live Stripe sandbox checkout + webhook flow.

Followed by the MVP track: TASK-MVP-001 (self-service signup/provisioning) → TASK-MVP-002 (shopper order end-to-end verification) → TASK-MVP-003 (merchant first-run store readiness) → TASK-MVP-004 (read-only production-launch-readiness investigation) → TASK-MVP-004A (application production hardening - `TRUSTED_PROXIES`/R20, central-domain config, Stripe-unavailable UX, `platform:production:check`, production-deployment runbook) → TASK-MVP-004B/003A/003B (real pilot deployment + backup system) → TASK-MVP-004 (real SMTP delivery) → TASK-MVP-005 (off-server backup sync to Cloudflare R2) → TASK-MVP-006 (production deployment source-of-truth safeguards, then public signup abuse protection via Cloudflare Turnstile) → TASK-MVP-007 (managed merchant onboarding, public signup disabled by default).

Every task closed with a full evidence-based report (real HTTP requests, real MySQL, real Redis, real queued jobs — no mocking of this project's own infrastructure) before commit, and every commit was individually approved.

# Open Risks

(Full detail in `RISK_REGISTER.md`; only the still-genuinely-open items, reclassified by this review:)

- **R19** — **RESOLVED (TASK-MVP-001)**: self-service signup landed with a slug allowlist regex (`^[a-z0-9]([a-z0-9-]*[a-z0-9])?$`) validated before any tenant `id`/database name is derived - closed for every current tenant-creation path.
- **R20** — **CLOSED AT THE APPLICATION LEVEL (TASK-MVP-004A)**: `TRUSTED_PROXIES` (env-driven, falls back to `'*'` only when unset) replaces the hardcoded `trustProxies(at: '*')`. The real production proxy IP value is still an infrastructure decision (TASK-MVP-004B).
- **DECISION_LOG "HUMAN DECISION REQUIRED" #3** — **PARTIALLY RESOLVED (TASK-MVP-004A)**: wildcard TLS for the platform's own subdomains is confirmed sufficient for MVP (`docs/architecture/production-deployment.md`). True custom-domain-per-tenant SSL remains open/post-MVP.
- **R52** (new, TASK-MVP-004A, CLOSED) — `CheckoutController::store()` could expose a raw, unhandled `MissingProviderCredentialsException` when Stripe is intentionally unconfigured (the recommended pilot posture) - fixed via a global exception-render handler.
- R1 (full-page response cache tenant-safety) — config-level exposure closed (`RESPONSE_CACHE_ENABLED=false` by default), underlying `Webkul\FPC` hasher never audited — safe as long as it stays off; must be re-verified before ever enabling it.
- R3 (PhonePe cache key) — structurally resolved via the same mechanism as everything else, never independently verified, and PhonePe (India-specific) is not a relevant gateway for this vertical/market — safe to defer indefinitely.
- R8/R12 (Octane-related) — Octane is dormant/unconfigured and the recommendation is to keep it off through MVP — safe to defer.
- R13 (Sanctum/API guard unused) — no API planned for MVP — safe to defer.
- R38 (product-limit count-then-create race) — documented, no evidence of concurrent-creation as a realistic pattern for a small pilot — safe to defer.
- R49 (long-suite-duration real-worker/queue-draining test flake) — development/test-infrastructure only, reproduced and root-caused as environment-timing-related, not a Platform/tenancy defect — safe to defer.
- R50 (abandoned `booted()`-callback middleware-attachment technique for one specific route) — development-only, the shipped welcome-banner feature does not depend on it — safe to defer.
- R51 (Vite manifest gap for two Admin-theme placeholder assets, TASK-MVP-003) — **re-checked fresh during the MVP final readiness review (2026-08-16) and found NOT REPRODUCIBLE**: both `src/Resources/assets/images/product-placeholders/front.svg` and `.../icon-add-product.svg` resolve correctly through the real, currently-committed `public/themes/admin/default/build/manifest.json` (verified live via a direct `Vite::asset()` call in the same Docker test environment - no exception). Whatever caused the original observation is no longer present; recommend closing outright rather than carrying as an open risk. If it resurfaces during TASK-MVP-004B, a normal `npm run build` for the Admin theme is the correct fix, not an application code change.

Everything else in the register (R1-R60, R63, R64-R69, R71, R72, minus the above) is RESOLVED/CLOSED with live evidence, including R68 (TASK-MVP-008, central-host exception rendering safety), R71 (TASK-MVP-012, theme-content per-locale seeding gap), and R72 (TASK-MVP-013, `LimitExceededException` 422-rendering regression - fixed and production-verified live via TASK-MVP-014's real 422 + zero-persistence proof on `arabic-mvp-check`). R61/R62 (found during TASK-MVP-003B) and R70 (found as a byproduct of TASK-MVP-008 - Platform Admin's unauthenticated redirect lands on the shop customer login instead of its own) remain open, Low priority, deliberately not folded into any subsequent task's scope - see RISK_REGISTER.md.

- **R73** (found TASK-MVP-016, implemented TASK-MVP-018) — every tenant's order/shipment/invoice emails send "From: Technify" because the tenant-scoped `core_config` row Bagisto's own unmodified `Core::getSenderEmailDetails()` already reads (`emails.configure.email_settings.sender_name`) was simply never seeded with a value. TASK-MVP-018 seeds it automatically at managed-onboarding time plus a `platform:tenants:repair-sender-identity` backfill command - display NAME only (Model A), `sender_email`/Reply-To never touched. **The first production deployment found a real second bug**: the original raw-DB write bypassed Prettus L5 Repository's cache invalidation (stock Bagisto config enables caching specifically for `CoreConfigRepository`), so a tenant already read before being seeded kept a stale cached "Technify" indefinitely. Fixed by writing through `CoreConfigRepository::create()` instead (the same path the real Admin Configuration UI uses), with 2 new regression tests reproducing the exact bug. 17 new tests + 69 existing regression tests all passing; zero `packages/Webkul` changes. **OPEN / IMPLEMENTED - PENDING PRODUCTION RE-VERIFICATION** - fix committed/pushed, not yet re-deployed; see RISK_REGISTER.md for full evidence.
- **R74** (new, TASK-MVP-016) — the real storefront checkout state/governorate dropdown (`Core::groupedStatesByCountries()`) bypasses Astrotomic Translatable via a raw query, always showing one fixed language (Arabic, for Palestine's newly-seeded governorates) regardless of the shopper's active locale. Needs a `packages/Webkul` change to fix properly - deferred. OPEN, Low.
- **R75** (new, TASK-MVP-016) — the Admin Orders DataGrid listing's date column bypasses `Core::formatDate()` (unlike the single-order view and all order emails), so it stays UTC-displayed even though `channels.timezone` is now set to `Asia/Hebron` for Palestine-first tenants. Needs a `packages/Webkul` change to fix properly - deferred. OPEN, Low.
- **R76** (new, TASK-MVP-017) — a real production incident: `web` (nginx) went 10 days without being rebuilt while `app` was redeployed 3 times (build-time `COPY --from=app` in `Dockerfile.production`, never a runtime-shared volume), silently serving stale JS/CSS until Admin's manifest referenced a file `web` never had - breaking Merchant Admin Login/Reset Password across every tenant. CLOSED for the live incident (fixed, human-verified during TASK-MVP-016). Durable safeguard (`docker/production/deploy.sh` + two new `platform:production:check` rows) implemented, committed, pushed, deployed, and production-verified by TASK-MVP-017 (including a real first canonical deployment run, app+web-together consistency proof, and mysql/redis-untouched proof).
- **R77** (new, TASK-OPS-018/TASK-OPS-019, CLOSED via TASK-MVP-018's own deployment) — production root filesystem reached 94% used (~30GB under `/var/lib/docker`), root-caused to `Dockerfile.production` copying the whole repo before `npm install`/`composer install`, compounded by zero disk lifecycle policy in `deploy.sh`. TASK-OPS-018's manual cleanup reduced usage to 76%; TASK-OPS-019 fixed the Dockerfile layer ordering (proven via a real cache-hit test) and added a bounded, age-filtered cleanup policy, a Docker-root-aware fail-closed disk pre-flight gate, a shared `flock` concurrency lock, and a one-deploy-consistent rollback pair. **CLOSED** - the first production deployment (commit `bd3e2b4826f1cbf2059bd3eef360db267207fa79`) verified every mechanism live (usage improved 76%→66%, rollback pair confirmed exact, lock contention proven both directions); the required subsequent-deploy evidence then arrived via TASK-MVP-018's own real, source-changing deployment (commit `a283d25ea944f85dd91936da33a648a35cce711f`) - dependency layers cache-hit, usage stayed exactly flat 66%→66%, rollback metadata correct, mysql/redis untouched. Daily cron backstop remains installed regardless.

**MVP final readiness review (2026-08-16)**: no application-level blocker found. See the "MVP FINAL READINESS REPORT" delivered at this checkpoint for the full journey-by-journey evidence (merchant onboarding, merchant operations, shopper checkout, Platform Owner - all READY FOR PILOT), the mail-under-`sync` investigation (order placement succeeds independently of mail delivery - `Webkul\Shop\Listeners\Base::prepareMail()` and `Webkul\Shop\Listeners\Order::afterCreated()` both already catch and log any mail exception, including an unconfigured-SMTP `RuntimeException`, without affecting the HTTP response - confirmed via direct source reading of Bagisto's own unmodified code, a structural guarantee, not merely observed behavior), and the R51 re-check above. **Application MVP feature scope is now frozen for the pilot** - see "Post-MVP / explicitly deferred" below for what requires a new product decision before being pulled back in. Remaining work is entirely TASK-MVP-004B (infrastructure/deployment), not further application development.

# MVP Gaps

1. ~~No merchant self-service signup/tenant creation.~~ **DONE (TASK-MVP-001).**
2. ~~No verified shopper order flow.~~ **DONE (TASK-MVP-002).**
3. ~~No onboarding polish.~~ **DONE (TASK-MVP-003 - welcome banner; owner-chosen credentials already landed in TASK-MVP-001).**
4. **Real-domain/production deployment posture — application half DONE (TASK-MVP-004A: R20 closed, central-domain config, Stripe-unavailable UX, production-check command, runbook). Actual infrastructure (TASK-MVP-004B) not started: no real domain/server/DNS/TLS/SMTP/Redis exists anywhere outside Docker-local.**

**Application MVP development is complete.** The 2026-08-16 final readiness review found zero application-level blockers across every real MVP journey (merchant onboarding, merchant operations, shopper checkout, Platform Owner). Everything remaining is TASK-MVP-004B infrastructure/deployment work, not further coding.

# Remaining MVP Roadmap

~~TASK-MVP-001 — Merchant Self-Service Signup & Automatic Provisioning.~~ **DONE, committed, approved.**

~~TASK-MVP-002 — Storefront Shopper Order End-to-End Verification.~~ **DONE, committed, approved.**

~~TASK-MVP-003 — Merchant Store Essentials & First-Run Readiness.~~ **DONE, committed, approved.**

~~TASK-MVP-004 — Production Launch Readiness.~~ **Investigated (read-only), then split (see below).**

**TASK-MVP-004A — Application Production Hardening.** Why: close what's fixable purely at the code/config layer without needing a real server/domain yet. Outcome: `TRUSTED_PROXIES` (closes R20 at the application level), `PLATFORM_CENTRAL_DOMAINS` (explicit, separate from `PLATFORM_BASE_DOMAIN`), a clean Stripe-unavailable checkout UX (no more raw unhandled exception), `platform:production:check`, `docs/architecture/production-deployment.md`. Depends on: nothing new architecturally. Size: S. **DONE - pending commit approval as of this review.**

**TASK-MVP-004B — Actual Pilot Deployment.** Why: TASK-MVP-004A hardens the application but does not deploy it anywhere reachable by a real merchant. Outcome: a real server, real domain, real DNS, wildcard TLS, real Redis, a working SMTP fallback, a production `docker-compose.yml` (or equivalent) including the currently-missing `storage/` persistent volume, backup script + scheduling, first-production-bootstrap executed for real. Depends on: TASK-MVP-004A (done) + human decisions (real domain, hosting provider/server, whether real mail must work for the first external pilot merchant). Size: not meaningfully estimable in engineering-task terms - gated on infrastructure/ops execution, not code. **DONE** - real Oracle Cloud server, real domain (`technify.dev`), real TLS, real backups, real SMTP all live (see "Current Capabilities" above).

~~TASK-MVP-005 — Off-Server Backup Sync (Cloudflare R2).~~ **DONE, committed, approved.** Closes R64.

~~TASK-MVP-006 — Production Deployment Source-of-Truth Safeguards, then Public Signup Abuse Protection (Cloudflare Turnstile).~~ **DONE, committed, approved.** Closes R65/R66.

~~TASK-MVP-007 — Managed Merchant Onboarding / Disable Public Signup.~~ Why: limited initial server capacity + real per-tenant cost + a small expected initial merchant count make curated, operator-driven onboarding the right posture for the initial commercial/pilot phase - not a readiness gap, a deliberate choice. Outcome: `PUBLIC_SIGNUP_ENABLED` flag (`/join` 404s cleanly when off, zero side effects, signed retry unaffected); Platform Admin "Create Merchant" flow reusing `MerchantOnboarding::register()` verbatim; random-then-discarded temporary password + real Admin password-reset broker for activation (no plaintext password ever operator-visible or persisted); `platform:production:check` `Public signup` row; `docs/architecture/onboarding.md`. Depends on: nothing new architecturally - reuses TASK-MVP-001/004/006's existing services. Size: M. **DONE, committed, approved, proven live in production.**

~~TASK-MVP-008 — Fix R68: Central-Host Exception Rendering Safety.~~ Why: found live as a byproduct of TASK-MVP-007's own R67 hotfix - a broader-than-signup defect where any central-domain request reaching the app with tenancy never initialized (a CSRF failure, an unmatched/verb-mismatched route, a throttle rejection) crashed with a raw 500 instead of a clean error response, because Webkul's own fallback error view unconditionally queries tenant-only tables. Outcome: `Platform\Tenancy\Exceptions\CentralSafeExceptionHandler`, a `Container::extend()` decorator around whatever handler Webkul bound - zero `packages/Webkul/*` changes. Proven live in production (419/405/429 all render cleanly; unknown-domain, real tenant, and Platform Admin behavior all unaffected; `platform:production:check` all-PASS). A self-found regression (the first deployed version incorrectly 500'd `AuthenticationException`, breaking Platform Admin's own unauthenticated redirect) was caught and fixed within the same task, before being reported complete. R70 (a separate, pre-existing, low-severity wrong-redirect-TARGET defect, unrelated to this fix) was found as a byproduct and deliberately left open. Depends on: nothing new architecturally. Size: M. **DONE, committed, approved, proven live in production.** Closes R68.

~~TASK-MVP-009 — Source Safety / Release Checkpoint.~~ Why: 28 commits had accumulated ahead of `origin/2.4`, never pushed - before pushing, a strictly read-only audit was warranted (secrets/history, runtime artifacts, `packages/Webkul/*` untouched, composer/lint/test health, production `APP_COMMIT` match, backup health, CI-trigger/cost review, open-risk review, this file's own drift). Outcome: everything came back clean; this file's own "current state" sections were found materially stale (this update) and corrected as a narrow, documentation-only exception to the checkpoint's own read-only scope. Depends on: nothing new architecturally. Size: S. **DONE, committed, approved.** `2.4` pushed to `origin`.

~~TASK-MVP-010 — Next Product Priority Checkpoint.~~ Why: with the platform live and source safety confirmed, a read-only product-prioritization review was needed before starting further engineering work. Outcome: a business-value classification of every open item (R61/R62/R70/R49, DMARC, legal pages, async provisioning, billing collection, custom domains, etc.) and a single recommended next task (a lightweight merchant onboarding checklist) - no code changed, purely a planning checkpoint. **DONE.** Superseded in practice by TASK-MVP-012 being chosen next instead.

~~TASK-MVP-011 — Disable GitHub Actions / Local-First Testing.~~ Why: GitHub Free-tier Actions minutes are limited, and several upstream Bagisto CI lanes already had a known, unrelated `bagisto:install` incompatibility with this fork. Outcome: all 7 `.github/workflows/*.yml` switched to `workflow_dispatch`-only (no more automatic `push`/`pull_request`/tag triggers); local-first testing established as official policy (`docs/implementation/ci-testing.md`, `DECISION_LOG.md` C83). Depends on: nothing new architecturally. Size: S. **DONE, committed, approved, pushed.**

~~TASK-MVP-012 — Arabic-First Merchant & Storefront Experience.~~ Why: the initial target market is Arabic-speaking merchants/customers - a deliberate product decision that new stores should be Arabic-first by default, not merely Arabic-capable. Outcome: `TenantProvisioner::ensureArabicLocaleSeeded()`/`ensureArabicThemeContentSeeded()`, `Platform\Tenancy\Http\Middleware\SetTenantAdminLocale` (new), `OwnerActivationMailer` locale handling, `config/translatable.php` fallback changed to `ar` - see "Current Capabilities" above and `docs/architecture/localization.md` for the full design. Depends on: nothing new architecturally - reuses the existing provisioning pipeline and the R69/C81 capture/set/restore idiom. Size: M. **DONE, DEPLOYED, PRODUCTION-VERIFIED, CLOSED** (deployed and verified by TASK-MVP-014 via the real `arabic-mvp-check` tenant).

~~TASK-MVP-013 — Fix R72: `LimitExceededException` 422→500 Regression.~~ Why: found live as a byproduct of TASK-MVP-012's own full regression run - confirmed via `git stash` bisection to already exist on the clean `origin/2.4` baseline, unrelated to that task, but blocking its deployment per the product owner's own framing. Root cause confirmed by direct Laravel framework source reading: `Container::extend()` (the mechanism `CentralSafeExceptionHandler`/TASK-MVP-008 uses) applies its decorator to a freshly-resolved handler *before* `Container::resolve()` fires `afterResolving()` callbacks, so R36's original `Handler::renderable()` registration (which type-checks against `Illuminate\Foundation\Exceptions\Handler`) silently never matches a decorator that implements the handler contract directly instead of extending that class. Outcome: a new shared trait, `Platform\Plans\Exceptions\Concerns\RendersAsEntitlementFailure` (exception-owned `render()`, structurally immune to registration-order/object-identity issues - the same class of guarantee already relied on for `TenantNotReadyHttpException`), used by `LimitExceededException`/`NoPlanAssignedException`/`FeatureNotConfiguredException`/`FeatureTypeMismatchException`; the dead `EnforcementServiceProvider::registerExceptionRenderer()` removed. Exact historical HTTP contract preserved byte-for-byte (422, same messages). Zero `packages/Webkul/*` changes. Depends on: nothing new architecturally. Size: S. **DONE, committed. Deployed and production-verified by TASK-MVP-014** (live 422 + zero-persistence proof on `arabic-mvp-check`). Closes R72.

~~TASK-MVP-014 — Deploy Arabic-First + R72 Fix / Real Arabic Production Verification.~~ Why: TASK-MVP-012 and TASK-MVP-013 were both implemented and committed but never deployed - a real production deployment plus live verification was needed before either could be considered done. Outcome: both commits deployed via the standard R65/C76 source-of-truth process (host tree updated first, `APP_COMMIT` written, image rebuilt, container force-recreated); one real production tenant (`arabic-mvp-check`) created through the genuine Platform Admin "Create Merchant" HTTP flow (never a shortcut); comprehensive live verification covering locale/channel/theme DB state, Merchant Admin and storefront `lang="ar" dir="rtl"` rendering, a real Arabic category/product created through the real Admin UI, English confirmed as a genuine switchable secondary locale, activation-email language/host correctness (human-confirmed), and a live, safely-reversible R72 422 + zero-persistence proof using a dedicated disposable plan reassigned only to `arabic-mvp-check`. All four pre-existing English tenants (`pilot-smoke`/`test1`/`thaertest`/`mvp007-check`) re-verified completely unchanged before and after. `platform:production:check` all-PASS throughout. Two findings investigated and reported but correctly left unfixed (out of scope): the pre-existing, unrelated R62-signature log entry (predates this deployment by a full day), and a `packages/Webkul/Admin` Blade error reachable only through a malformed raw-HTTP request my own first curl attempt sent (never reachable through the real browser form). Depends on: TASK-MVP-012 + TASK-MVP-013. Size: M. **DONE.** Closes TASK-MVP-012 and R72 as production-verified.

**TASK-MVP-015 — Merchant Onboarding Checklist / First-Real-Merchant Readiness.** Why: with the technical MVP production-verified, the near-term product strategy is curated managed onboarding (a limited number of merchants, operator-created) rather than further infrastructure work - the goal is making onboarding the FIRST REAL MERCHANT safe, repeatable, and hard to get wrong, not building a CRM/wizard/workflow engine. Outcome: a read-only audit (Part A-J) found no missing automation - provisioning/Arabic-first/activation are already fully automatic and production-proven - but found Platform Admin's tenant page showed neither owner identity nor clickable storefront/Admin URLs, and no canonical operator checklist existed anywhere. Implemented the audited Option C/Hybrid minimum: `TenantController::show()`/`tenants/show.blade.php` gained owner name/email, clickable storefront + Merchant Admin URLs, and a small DERIVED-ONLY "Onboarding Status" panel (tenant Ready, domain configured, owner identity recorded, plan/subscription consistent, Arabic locale configured - each computed live from existing `tenants`/`plans`/`subscriptions` rows and, for the Arabic check only, this one tenant's own database via `$tenant->run()`) - zero new persistence, zero new table. Deliberately does NOT claim to know payment/shipping/tax/catalog/checkout/activation readiness - those stay manual, human-verified, recorded in the new canonical `docs/operations/merchant-onboarding-checklist.md` (10 sections, `[Automated]`/`[Operator]`/`[Merchant]`/`[Verify]` tagged, includes an Arabic handoff message template). A separate Palestine-readiness audit (currency/address/timezone/tax/payment/shipping) was performed and classified but deliberately NOT implemented - see RISK_REGISTER.md/DECISION_LOG.md for the classification, to be picked up by a dedicated future task. Zero `packages/Webkul/*` changes. Depends on: nothing new architecturally - reuses existing tenant/plan/subscription/locale state. Size: S.

**TASK-MVP-016 — Palestine Readiness / Defaults.** Why: TASK-MVP-015's own audit classified currency/country/timezone/governorate/address/payment/shipping/tax items for Palestine - this task implements the approved subset. Outcome: five new idempotent `TenantProvisioner` steps (`ensurePalestineCurrencySeeded()`, `ensurePalestineGovernoratesSeeded()`, `ensurePalestineTimezoneSet()`, `ensurePalestineAddressDefaultsSeeded()`, `ensurePalestineCashOnDeliveryEnabled()`) plus one new small support class (`Platform\Tenancy\Support\PalestineGovernorates`, the authoritative 16-governorate list) and one global config lever (`config('app.default_country')` = `PS`, via a new `APP_DEFAULT_COUNTRY` env var - see "Current Capabilities" above for the full outcome and `docs/architecture/palestine-readiness.md` for the complete design, including three formally-recorded, deliberately-deferred findings this task's own audit surfaced (R73/R74/R75) and the disclosed governorate-code provenance caveat. Zero `packages/Webkul/*` changes. Explicitly deferred, not implemented: USD secondary currency/exchange-rate automation, governorate/city-based delivery pricing, a real local Palestinian payment gateway, per-tenant mail-sender identity (R73), the two `packages/Webkul`-gated gaps (R74/R75), any tax rate, any shipping fee. Depends on: TASK-MVP-012 (Arabic-first) + TASK-MVP-015 (its own audit). Size: M.

**TASK-MVP-017 — Eliminate app/web Production Asset Drift.** Why: a real production incident discovered during TASK-MVP-016's own verification (Merchant Admin Login/Reset Password inert across every tenant for 10 days, root-caused to `web`'s nginx image never being rebuilt while `app` was redeployed 3 times) confirmed a serious deployment-process defect that could recur silently. Outcome: see the "Current Capabilities" bullet above and RISK_REGISTER.md R76/DECISION_LOG.md C92 for the full design - a canonical `docker/production/deploy.sh` (always rebuilds/recreates `app` AND `web` together, runs `platform:production:check` as its own final gate) plus two new deterministic `platform:production:check` rows (`Admin static assets`/`Shop static assets`) that verify the current Vite-manifest assets are actually served by `web`, never a hardcoded filename. Zero `packages/Webkul/*` changes; zero business-functionality change. Depends on: nothing new architecturally - reuses the existing R65/C76 source-of-truth process and `platform:production:check` command. Size: S. **DONE, committed, pushed, deployed, and production-verified.**

**TASK-OPS-018 — Production Disk Space Audit + Targeted Cleanup.** Why: production root disk usage reached 94% used, flagged urgent, before any new feature/MVP work. Outcome: a strictly read-only audit (`df`, `docker system df -v`, `/var/lib/docker` breakdown, backup/log/cache inventory) found 306 BuildKit cache entries (13.12GB, 100% reclaimable, zero active) and 35 images (13.24GB, 90% reclaimable) as the dominant consumers, MySQL/Redis proven to be bind-mounted (never Docker volumes), and ~30GB of usage entirely unrelated to the estore application (`/home/ubuntu` personal dev tooling, `/www` a third-party hosting panel) explicitly left untouched. An approved, targeted cleanup (`docker builder prune -f` + `docker image prune -f`, no `-a`, no `docker system prune`, no volumes) reduced usage to 76% - proven via before/after container-identity checks (all 4 containers' IDs/StartedAt unchanged), a passing `platform:production:check`, and live smoke checks. A one-off rollback pair (`estore-app:rollback-pre-mvp017`/`estore-web:rollback-pre-mvp017`) was manually tagged from the pre-TASK-MVP-017 images before pruning. Depends on: nothing new architecturally. Size: S. **DONE, executed live in production, approved as complete.**

**TASK-OPS-019 — Production Docker Disk Hygiene Design + Implementation.** Why: TASK-OPS-018 revealed a structural gap (no disk lifecycle policy at all) - this task root-caused and fixed the underlying mechanism rather than relying on periodic cleanup alone. Outcome: see the "Current Capabilities" bullet above and RISK_REGISTER.md R77/DECISION_LOG.md C93 for the full design - `Dockerfile.production`'s dependency-install layers (`npm install` ×2, `composer install`) now cache on manifest content instead of the whole repository (proven via a real local cache-hit test), `deploy.sh` gained a fail-closed disk pre-flight gate and a canonical, deploy-consistent rollback pair (`estore-app:rollback`/`estore-web:rollback` + `ROLLBACK_COMMIT`), and cleanup is now age-bounded (7 days) rather than unconditional, shared between `deploy.sh` and a new `docker/production/docker-disk-hygiene.sh` script intended for an independent cron backstop. Zero `packages/Webkul/*` changes; zero business-functionality change. Depends on: TASK-OPS-018's own audit findings. Size: M. **DONE, committed, pushed, deployed, and production-verified. R77 is now CLOSED - the required subsequent-deploy bounded-growth evidence arrived via TASK-MVP-018's own real production deployment - see RISK_REGISTER.md R77 for the full evidence.**

**TASK-MVP-018 — Per-Tenant Transactional Mail Sender Identity.** Why: R73 (found TASK-MVP-016) - every tenant's transactional email showed "From: Technify," a real, recurring, customer-facing trust/branding defect, deliberately pulled forward before the first real merchant onboarding rather than waiting for a real merchant complaint (explicit product decision, see DECISION_LOG.md). Outcome: see the "Current Capabilities" bullet above and RISK_REGISTER.md R73 for the full design/evidence - the audit found the tenant-aware mail architecture already existed unmodified in `packages/Webkul` (`Core::getSenderEmailDetails()`); the fix only seeds the `core_config` row it reads (`TenantProvisioner::seedSenderIdentity()`, called from `MerchantOnboarding::attempt()` at provisioning time) plus a `platform:tenants:repair-sender-identity` backfill command, both Platform-owned, both idempotent, both never touching `sender_email` (Model A: display name only, platform-controlled address, no Reply-To). 17 new tests + 69 existing regression tests all pass; zero `packages/Webkul/*` changes. Depends on: none (R73 was already fully audited/designed as its own checkpoint before implementation). Size: S. **IMPLEMENTED, committed, pushed, and deployed once - that first real deployment found a genuine production bug (a raw DB write bypassing Prettus repository cache invalidation, causing a stale "Technify" to persist even after the correct DB row existed), root-caused via direct production diagnosis, and corrected (write path now goes through `CoreConfigRepository::create()`, with 2 new regression tests reproducing the exact bug). The correction is committed/pushed; NOT YET re-deployed as of this update. R73 remains open pending re-deployment, one documented one-time production cache invalidation for `palestine-mvp-check`, and real delivered-message re-verification.**

**Next in sequence (not yet started, listed here for continuity)**:
1. ~~TASK-MVP-015~~ / ~~TASK-MVP-016~~ / ~~TASK-MVP-017~~ / ~~TASK-OPS-018~~ / ~~TASK-OPS-019~~ (all above) / TASK-MVP-018 (implemented, deployed once, a real cache-invalidation bug found and fixed, pending re-deployment - see above).
2. **First complete real-merchant onboarding/store-launch exercise** - use the new checklist end-to-end for an actual real merchant, not another internal verification tenant. Requires TASK-MVP-016 to be deployed first (not yet deployed as of this update - see "Current Capabilities" above).
3. **Low-priority technical cleanup** (R70, R62) - only after the above, unless either's severity changes. R49 remains deferred/testing-related, not reprioritized ahead of merchant readiness.

**Post-MVP / explicitly deferred** (do not pull forward without a real merchant need):
- Real (non-Stripe) payment gateway integration — explicit product-owner decision: Palestine payment options are limited, pending bank/provider coordination, not a blocker.
- Refund execution, additional Stripe webhook event types, invoices, automatic renewal, failed-payment suspension policy.
- Custom-domain-per-tenant (BYO domain) workflow — subdomain-of-platform-domain is sufficient for MVP.
- Onboarding wizard / store branding UI beyond stock Bagisto Admin settings.
- Advanced RBAC, accounting dashboards, usage metering/analytics.
- Product-limit race-condition hardening (R38) — no evidence it's needed yet.
- Octane adoption.
- Off-server backup sync (S3/another host) — TASK-MVP-003A implemented real daily backups (central DB + every tenant DB + tenant files, restore-proven) but deliberately left the destination on the same physical server; see docs/implementation/backup-and-recovery.md "Known limitations". **DONE (TASK-MVP-005)** - see the "Off-server backup sync" bullet above; this line kept only because renumbering/removing entries elsewhere in this document set is avoided by convention.
- **Async merchant provisioning / immediate "Creating Your Store" progress UX** (TASK-MVP-006) — real production signup currently takes ~20-30 seconds between form submission and redirect, since `TenantProvisioner::provision()` (physical database creation, full schema migration, seeding, Admin/Subscription setup) runs synchronously inside the request. Observed live, not measured as a defect - the current MVP's correctness does not depend on this being fast, only correct, and it is. Revisit only if real merchant drop-off during this wait is observed as an actual problem, not preemptively; a real fix would need background job dispatch + a polling/progress UI, a materially larger change than this note. Platform Admin's own managed "Create Merchant" action (TASK-MVP-007) has the identical latency, for the identical reason - unchanged by that task, explicitly out of its scope.
- **Re-enabling public self-service signup** (TASK-MVP-007) — `PUBLIC_SIGNUP_ENABLED=false` in production is a deliberate posture for the initial commercial/pilot phase (limited capacity, real per-tenant cost, curated onboarding preferred while the merchant count is small), not a missing feature - the full `/join` implementation (including Turnstile) is preserved and immediately re-enable-able via one config flag. Flipping it back on requires a real readiness review first (capacity, an abandoned/never-activated-tenant lifecycle policy, abuse-control posture beyond Turnstile if volume grows, commercial/pricing policy) - see `docs/architecture/onboarding.md` "Re-enabling public signup later".
- **USD secondary currency / automated exchange rates** (TASK-MVP-016) — deferred until Bagisto's `exchange-rate:update` mechanism is verified correct in this multi-tenant fork's scheduler context; enabling it without that verification risks `Core::convertPrice()` silently showing an unconverted number. See DECISION_LOG.md C88.
- **Delivery/shipping pricing by governorate or city** (TASK-MVP-016) — Bagisto's Flat Rate carrier is a single fixed amount only; per-region delivery pricing would be new functionality, not a config change. Not needed for a first merchant.
- ~~Per-tenant mail-sender identity (TASK-MVP-016, RISK_REGISTER.md R73)~~ — **no longer deferred.** Implemented in TASK-MVP-018, pulled forward deliberately before the first real merchant onboarding: the "Technify" sender identity on every tenant's transactional email was already-observed evidence of a real, recurring customer-facing trust defect, not something requiring a real merchant complaint first (explicit product decision, see DECISION_LOG.md). Pending production verification - see RISK_REGISTER.md R73 and "Current Capabilities" below.

# Deferred Post-MVP Work

See the "Post-MVP / explicitly deferred" list immediately above — treat that list as authoritative for what NOT to pull into MVP scope without a concrete, real merchant-driven reason.

# Next Task

**TASK-MVP-018 (Per-Tenant Transactional Mail Sender Identity) is implemented, committed, pushed, and deployed once - that first deployment found a real production bug (raw DB write bypassing Prettus repository cache invalidation) which is now root-caused, fixed, committed, and pushed, but NOT YET re-deployed.** See the "Current Capabilities" bullet above and RISK_REGISTER.md R73/DECISION_LOG.md for the full design/evidence. Next step for this task is a re-deployment + the documented one-time `palestine-mvp-check` cache invalidation + real delivered-message re-verification, pending explicit approval. TASK-OPS-019 (Production Docker Disk Hygiene) is committed, pushed, deployed, and **R77 is now CLOSED** - the required subsequent-deploy bounded-growth evidence arrived via TASK-MVP-018's own real deployment - see RISK_REGISTER.md R77/DECISION_LOG.md C93. TASK-OPS-018 (the disk audit + targeted cleanup this depends on) is fully complete, executed live in production, and approved. TASK-MVP-017 (Eliminate app/web Production Asset Drift) is DONE, committed, pushed, deployed, and production-verified. TASK-MVP-015 (merchant onboarding checklist) and TASK-MVP-016 (Palestine readiness/defaults) remain implementation-complete as of the last update to those sections - see "Remaining MVP Roadmap" above for what each covers and note that the status lines there may not reflect every task's latest real deployment state (verify against `git log`/production directly before relying on them, per this file's own "How To Continue" guidance below). The agreed near-term sequence, in order: (1) TASK-MVP-015 (done), (2) TASK-MVP-016 (done locally, pending deploy approval per its own section above), (3) TASK-MVP-017 (done, deployed, production-verified), (4) TASK-OPS-018 (done, production-verified), (5) TASK-OPS-019 (done, deployed, R77 CLOSED), (6) TASK-MVP-018 (implemented, one production bug found and fixed, pending re-deployment/re-verification, R73 open), (7) a first complete real-merchant onboarding/store-launch exercise using the new checklist and Palestine-first defaults, (8) low-priority technical cleanup (R70/R62) afterward unless severity changes. R49 remains deferred/testing-related, not reprioritized. Do not start any of these without explicit instruction.

The platform is live in production (`technify.dev`) with managed-only merchant onboarding; real merchants are added by the operator through Platform Admin's "Create Merchant" action, and new tenants are Arabic-first by default. No task should be started without explicit instruction - see this document's own "How To Continue In A New Chat" note below.

# How To Continue In A New Chat

Read this file first, then `IMPLEMENTATION_PLAN.md`, `DECISION_LOG.md`, and `RISK_REGISTER.md` as needed for historical detail on any specific past task. Verify code before implementation — this file and those documents describe the state as of the last review; don't rely solely on historical task reports without checking the current repository (`git log`, `git status`, and the actual files) first. Never modify `packages/Webkul/*`. All new SaaS code goes in `packages/Platform/*`. Real integration tests only, no mocking of this project's own database/filesystem/cache/queue (Stripe SDK's own official test seam for mocking Stripe's network layer is the one accepted exception). Each task ends with a full evidence-based report and stops for explicit approval before commit — this project has never committed or pushed without it, and every task in this document set followed that pattern exactly.
